export enum Routes {
  Gallery = "/gallery",
  Home = "/",
  RaceDetails = "/races/[id]",
  Races = "/races",
  Results = "/results",
  Settings = "/settings",
}

export enum RoutesDynamicKey {
  RaceId = "[id]",
}

export enum RouteLayoutSegments {
  Dashboard = "dashboard",
}
