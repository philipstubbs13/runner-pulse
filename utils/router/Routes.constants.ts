export enum Routes {
  Gallery = "/gallery",
  Home = "/",
  RaceDetails = "/races/[id]",
  Races = "/races",
  Results = "/results",
}

export enum RoutesDynamicKey {
  RaceId = "[id]",
}

export enum RouteLayoutSegments {
  Dashboard = "dashboard",
}
